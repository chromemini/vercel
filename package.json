{
  "name": "cf-bridge-five",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel Edge bridge for Cloudflare Workers AI (BYOK) — bypasses browser CORS for the YAP translation engine.",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "engines": {
    "node": ">=18"
  }
}